"""GTK3 view widgets for the grc-agent chat sidebar.

``chat_sidebar.py`` is the public facade (the ``ChatSidebar`` widget plus the
agent turn-loop orchestration). The widgets it composes live here, one focused
module each, so each is small and independently testable.
"""
