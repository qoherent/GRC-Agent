spec:
  name: lifecycle-gui-packaging
  type: judge
  primary_metric: code_quality_score
  direction: maximize

run_id: lifecycle-gui-packaging-2026-07-13
started_at: "2026-07-13T07:07:24-04:00"

baseline:
  timestamp: "2026-07-13T07:13:00-04:00"
  gates:
    ruff_violations: 2
    test_collection_errors: 1
    concrete_antipatterns: 1
  diagnostics:
    loc_web_py: 1323
    loc_canvas_app_py: 657
    loc_panel_js: 1325
    regions_written: 27
  notes: |
    Baseline gate failures:
    1. ruff_violations=2: web.py lines 1175 and 1196 exceed 100-char limit (E501,
       suppressed by config but still present)
    2. test_collection_errors=1: tests/test_unit.py imports BLOCK_COLUMN_MAX_ROWS
       from adapter.py — this symbol was removed from adapter.py, breaking all
       test_unit.py collection. Affected test: test_block_placement_column_overflow.
    3. concrete_antipatterns=1: web.py:1311 has time.sleep(1.0) in synchronous main()
       (the startup browser-open loop), which blocks the process for 1 second before
       checking if the server is accepting connections.

best:
  iteration: 1
  is_baseline: false
  commit: 25532e3
  gates:
    ruff_violations: 0
    test_collection_errors: 0
    concrete_antipatterns: 0
  judge_score: null  # judge run pending

experiments:
  - id: exp-1
    hypothesis: >
      Fix stale adapter imports in test_unit.py (_footprints_overlap renamed to
      _rects_overlap, BLOCK_COLUMN_MAX_ROWS removed) and fix two E501 lines in
      web.py.
    changes:
      - "tests/test_unit.py: remove BLOCK_COLUMN_MAX_ROWS import (symbol deleted from adapter.py)"
      - "tests/test_unit.py: replace _footprints_overlap with _rects_overlap (function renamed)"
      - "tests/test_unit.py: update all 4 call sites from (tuple, tuple) to (*tuple, *tuple)"
      - "tests/test_unit.py: rewrite test_change_graph_add_blocks_batch_wraps_column to test spiral non-overlap"
      - "src/grc_agent/web.py:1175,1196: split two lines exceeding 100-char limit"
    result:
      ruff_violations: 0   # was 2
      test_collection_errors: 0  # was 1
      concrete_antipatterns: 0   # was 1
    verdict: improvement  # all three gates cleared
    commit: 25532e3
    notes: >
      The stale import was the primary blocker — it prevented the entire
      test_unit.py module from collecting. _footprints_overlap was also renamed
      to _rects_overlap with a different call signature (two tuples → four scalars)
      at the same time BLOCK_COLUMN_MAX_ROWS was removed. Three stale symbols
      in total, all fixed in one pass.

hypothesis_backlog: []
