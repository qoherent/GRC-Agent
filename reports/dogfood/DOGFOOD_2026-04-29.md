# Dogfood Pass 2026-04-29

## Scope

- 12 held-out installed GNU Radio examples.
- 3 workspace fixture stand-ins because no private user `.grc` files are available in this workspace.
- Mix of llama.cpp-backed `chat` turns and direct public tool calls.
- Intake path: `reports/dogfood/dogfood_2026-04-29.jsonl` (ignored).
- Summary path: `reports/dogfood/dogfood_2026-04-29_report.json` (ignored).

## Result

- Total observations: 15.
- Clean observations: 13.
- One-off failures: 2.
- STOP_THE_LINE: 0.
- Unsafe mutation: 0.
- Raw YAML bypass: 0.
- Preview mutation: 0.
- Wrong-file overwrite: 0.
- Save/reload mismatch: 0.
- Repeated generic failure cluster: 0.

## Coverage

- Inspect/summarize: `peak_detector2`, `qtgui_vector_sink_example`, `channel_tone_response`.
- Validate: `matrix_interleaver`, `tags_to_pdu_example`, `noise_power`, workspace dual-sink fixture.
- Parameter edit: `demo_spec_an` `samp_rate` edit followed by validation.
- Preview: installed `vector_source_with_tags` and workspace fixture `samp_rate` previews.
- Save copy: `file_metadata_source` saved to `/tmp/grc-agent-dogfood/file_metadata_source_copy.grc`, then validated.
- State edit: `tagged_file_sink` disable request.
- Disconnect: `vector_source_with_tags` exact connection removal request.
- Rewire: vague `ofdm_loopback` rewire request.
- Retrieval: workspace fixture `search_grc` for `samp_rate`.

## Findings

- The vague OFDM rewire request clarified without tools. This is expected behavior.
- The installed `tagged_file_sink` state-edit attempt failed GNU validation before commit. This is a one-off `grcc_failure`, not a patch candidate.
- The installed `vector_source_with_tags` disconnect attempt failed GNU validation before commit. This is a one-off `grcc_failure`, not a patch candidate.
- The preview cases used `propose_edit`; no `apply_edit` was emitted for preview.
- The save-copy case wrote the requested copy and the saved copy validated.

## Decision

No runtime patch is justified from this pass. Continue collecting dogfood evidence and patch only for STOP_THE_LINE events or repeated generic failures across unrelated graphs.
