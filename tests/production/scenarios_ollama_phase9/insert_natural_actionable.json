{
  "schema_version": "2026-05-15.phase9-ollama-scenario-v1",
  "scenario_id": "insert_natural_actionable",
  "user_mode": "ollama_user",
  "graph_id": "fixture_random_bit_generator",
  "initial_user_prompt": "",
  "scenario_goal": "Return exactly this user request and nothing else: Please insert a throttle block on the connection from the random source to the existing throttle in the copied graph.",
  "allowed_user_behavior": [
    "Return exactly the natural insert request from the scenario goal."
  ],
  "forbidden_user_behavior": [
    "Do not add exact connection IDs, instance names, parameters, or schema field names.",
    "Do not mention internal tool names, JSON, schemas, or judge expectations."
  ],
  "max_user_turns": 1,
  "max_turns": 1,
  "allowed_capabilities": ["R4A_INSERT_BLOCK_ON_CONNECTION"],
  "expected_final_state": {
    "dirty": true,
    "blocks": {"present": ["blocks_throttle2_inserted"]},
    "connections": {
      "present": [
        "analog_random_source_x_0:0->blocks_throttle2_inserted:0",
        "blocks_throttle2_inserted:0->blocks_throttle2_0:0"
      ],
      "absent": ["analog_random_source_x_0:0->blocks_throttle2_0:0"]
    }
  },
  "expected_graph_delta": {
    "must_include": {
      "added_blocks": ["blocks_throttle2_inserted"],
      "removed_connections": ["analog_random_source_x_0:0->blocks_throttle2_0:0"]
    }
  },
  "expected_validation_status": "valid",
  "expected_save_load_behavior": {"save_expected": false, "load_expected": false},
  "forbidden_events": [
    "raw_legacy_tool_call",
    "raw_yaml_mutation",
    "preview_mutation",
    "failed_validation_commit",
    "unsafe_save",
    "unsafe_load",
    "original_graph_mutation"
  ]
}
