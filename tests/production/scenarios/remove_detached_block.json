{
  "schema_version": "2026-05-14.phase2-scenario-v1",
  "scenario_id": "remove_detached_block",
  "graph_id": "fixture_random_bit_generator_with_unused_var",
  "initial_user_prompt": "Remove the detached unused_var block from the copied graph.",
  "scripted_user_turns": [
    {
      "assistant_actions": [
        {
          "tool": "change_graph",
          "kwargs": {
            "dry_run": false,
            "user_goal": "Remove detached unused_var.",
            "operation_kind": "remove_block",
            "instance_name": "unused_var"
          }
        },
        {
          "text": "The detached block was removed on the copied graph and validation passed."
        }
      ]
    }
  ],
  "max_turns": 1,
  "allowed_capabilities": ["R4B_REMOVE_BLOCK"],
  "expected_final_state": {"dirty": true, "blocks": {"absent": ["unused_var"]}},
  "expected_graph_delta": {"must_include": {"removed_blocks": ["unused_var"]}},
  "expected_validation_status": "valid",
  "expected_save_load_behavior": {"save_expected": false, "load_expected": false},
  "forbidden_events": ["raw_legacy_tool_call", "raw_yaml_mutation", "preview_mutation", "failed_validation_commit", "unsafe_save", "unsafe_load", "original_graph_mutation"]
}
